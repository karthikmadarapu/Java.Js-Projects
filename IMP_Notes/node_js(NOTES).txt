<<<< INTRODUCTION TO NODE.JS >>>>
Node.js ==== is an application where its(runtime environment).
Also Node.js can be used to build backened services (API's).   
(Web Apps and Mobile Apps both run via help of the backened services).

There are multiple frameworks that can be used to run the backened, but why node?

Node.js Benefits and pros
-Great for prototyping and agile development.
- It's superfast and highly scalable.

Use of js is permanent in whole fullstack and MERN stack journey.


==================== ARCHITECTURE OF NODE.JS ========================

=> Every web browser has it's own engine to compile js code or Machine language and each brower has different types of their engines. for instance [chrome: v8 engine, edge: chakra and e.t.c] ;

 => Ryan Dhal the founder and the CEO of the NODE.JS created,  embedding the web Browser engines inside the c++ and called it as Node.exe.
=> his main idea was running the code or app / program outside the web browser.

=> we have different objects that reads and does certain tasks, 
fs.readFile()=== file systems, http.createServer() ==== to run the port number.

<<<<<<<<<< How Does The Node Even Works. >>>>>>>>>>>>>>>>>

Tip ==== Node is highly scalable because of it's non-blocking and asynchronous nature.

<<<<<<<<<<< NODE MODULE SYSTEM >>>>>>>>>>>>>>>>>>>>

 -There are builtin modules such as (os, fs, events, http)

==> Global objects
- console.log()
- setTimeout()
- clearTimeout()
- setInterval()
- clearInterval()

-- window is a scope. if you have to access a global object you can access via window scope.


<<<<<<<<<<<<<<< MODULES >>>>>>>>>>>>>>>>>>>>>>>>>>>
- every js application has it's main module, which acts as a main module.

---------------------------- How do you create a module ?---------------
- we do use the module.exports.xxx =  url / log // any thing that u export.


----------------------------How do you load a module? _----------------
- we use require function and put the file location where is it located in.
- we can also log it using .log() function like console.log() function.

step 1 - to export the module you use the export function 
step 2 - to load the module in another function we use the require function to get that exact function. 

--------------- Module wrapper function --------------------
- it's totally a backend that we dont use in day to day operations and , it runs and does certain  tasks under the hood and. we don't have to bother about it much. 

-------------path module ----------------
-path.parse()
- how do we use it ====  const path = require('path');
- path  outputs all the information about the current main module app. 
- it outputs in json. 



------------- The OS module ------------------
-the os module is always there with the node js and , we can use it by 

ex: const os  = require('os');

var totalMemory =  os.totalmem();
var freeMemory =  os.freemem();

console.log(`The total memory is : ${totalMemory}`);

console.log(`The free memory is : ${freeMemory}`);


- this is how we used to get the data / memory info in our computer.



----------------- File System module ---------
- fs module , has many modules in it and  it has both async and sync funcs , that we can use to display  many functions 

we just const fs = require('fs');
fs.func('./', function(err, files){
    if(err) console.log('Error: ', err);
    else console.log('success', files);
});


---------------- Event modules ---------------
EVENT =  a signal that something has happened.